spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5435/synthora}
    username: ${DB_USER:synthora_admin}
    password: ${DB_PASSWORD:postgres}

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    baseline-on-migrate: true
    out-of-order: true

logging:
  level:
    root: INFO
    com.synthora: DEBUG

jwt:
  secret: ${JWT_SECRET:SynthoraDevSecretKeyForJwtSigning2026!}
  expiration: ${JWT_EXPIRATION:86400000}